import type {Draft} from './schema';
export const escapeXML=(s:string)=>s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&apos;');
export function svgDiagram(d:Draft['diagrams'][number]){return `<svg xmlns="http://www.w3.org/2000/svg" width="800" height="500" viewBox="0 0 800 500" role="img"><title>${escapeXML(d.caption)}</title><defs><marker id="arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M0 0 L10 5 L0 10 Z" fill="context-stroke"/></marker></defs><rect width="800" height="500" fill="white"/>${d.shapes.map(s=>{const style=`stroke="${s.color}" stroke-width="2" fill="${s.fill}"`;switch(s.type){case 'line':case 'arrow':return `<line x1="${s.x}" y1="${s.y}" x2="${s.x2}" y2="${s.y2}" ${style} ${s.type==='arrow'?'marker-end="url(#arrow)"':''}/>`;case 'rect':return `<rect x="${s.x}" y="${s.y}" width="${s.width}" height="${s.height}" ${style}/>`;case 'ellipse':return `<ellipse cx="${s.x}" cy="${s.y}" rx="${s.width/2}" ry="${s.height/2}" ${style}/>`;case 'polyline':return `<polyline points="${s.points.map(p=>`${p.x},${p.y}`).join(' ')}" ${style}/>`;case 'text':return `<text x="${s.x}" y="${s.y}" font-family="Arial" font-size="20" fill="${s.color}">${escapeXML(s.text)}</text>`;}}).join('')}</svg>`;}
